name: PR自动检查和合并

# 权限配置 - 使用最大权限
permissions: write-all

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
      - master
      - develop
      - 'feature/**'
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR编号（用于手动调试）'
        required: true
        type: string

jobs:
  check-pr:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 设置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: '22.16.0'
          cache: 'npm'

      - name: 初始回复PR
        if: github.event_name == 'pull_request'
        continue-on-error: true
        run: |
          echo "=== PR自动检查开始 ==="
          echo "PR编号: ${{ github.event.pull_request.number }}"
          echo "提交者: ${{ github.event.pull_request.user.login }}"
          echo "目标分支: ${{ github.base_ref }}"
          echo "源分支: ${{ github.head_ref }}"
          echo ""
          echo "🔄 您的PR我们已经收到，正在运行自动化审查..."
          echo "稍后您将会收到具体审核报告，请耐心等待。"

      - name: 检查PR目标分支和文件变更类型
        id: check-files
        run: |
          # 检查是否为手动运行
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "这是手动运行，跳过PR检查，直接进行语法检查"
            echo "target_branch=manual" >> $GITHUB_OUTPUT
            echo "is_main_branch=false" >> $GITHUB_OUTPUT
            echo "doc_only=false" >> $GITHUB_OUTPUT
            echo "has_code_files=true" >> $GITHUB_OUTPUT
            echo "手动运行模式，将检查所有TypeScript文件"
            exit 0
          fi

          # 获取PR目标分支
          TARGET_BRANCH="${{ github.base_ref }}"
          echo "PR目标分支: $TARGET_BRANCH"
          echo "target_branch=$TARGET_BRANCH" >> $GITHUB_OUTPUT

          # 判断是否为主分支
          if [[ "$TARGET_BRANCH" == "main" || "$TARGET_BRANCH" == "master" || "$TARGET_BRANCH" == "develop" ]]; then
            echo "is_main_branch=true" >> $GITHUB_OUTPUT
            echo "这是向主分支提交的PR"
          else
            echo "is_main_branch=false" >> $GITHUB_OUTPUT
            echo "这是向非主分支提交的PR"
          fi

          # 获取PR中变更的文件列表
          git diff --name-only origin/${{ github.base_ref }}...HEAD > changed_files.txt

          # 检查是否只包含文档文件
          doc_only=true
          has_code_files=false

          while IFS= read -r file; do
            echo "检查文件: $file"
            # 检查是否为文档文件（.md, .txt, .rst, .doc, .docx, .pdf等）
            if [[ ! "$file" =~ \.(md|txt|rst|doc|docx|pdf|LICENSE|README)$ ]] && [[ ! "$file" =~ ^docs/ ]] && [[ ! "$file" =~ ^\.github/ ]]; then
              doc_only=false
              has_code_files=true
              echo "发现代码文件: $file"
            fi
          done < changed_files.txt

          echo "doc_only=$doc_only" >> $GITHUB_OUTPUT
          echo "has_code_files=$has_code_files" >> $GITHUB_OUTPUT

          # 输出变更文件列表用于调试
          echo "变更的文件:"
          cat changed_files.txt

      - name: 安装依赖（仅当有代码文件时或手动运行）
        if: steps.check-files.outputs.has_code_files == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          echo "安装根目录依赖..."
          npm install
          
          echo "安装客户端依赖..."
          cd client && npm install
          cd ..
          
          echo "安装服务端依赖..."
          cd server && npm install
          cd ..

      - name: TypeScript语法检查 - 客户端
        if: steps.check-files.outputs.has_code_files == 'true' || github.event_name == 'workflow_dispatch'
        id: tsc-check-client
        run: |
          echo "检查客户端TypeScript语法..."
          cd client
          if npx tsc --noEmit; then
            echo "client_check=success" >> $GITHUB_OUTPUT
            echo "✅ 客户端TypeScript语法检查通过"
          else
            echo "client_check=failed" >> $GITHUB_OUTPUT
            echo "❌ 客户端TypeScript语法检查失败"
            exit 1
          fi

      - name: TypeScript语法检查 - 服务端
        if: steps.check-files.outputs.has_code_files == 'true' || github.event_name == 'workflow_dispatch'
        id: tsc-check-server
        run: |
          echo "检查服务端TypeScript语法..."
          cd server
          if npx tsc --noEmit; then
            echo "server_check=success" >> $GITHUB_OUTPUT
            echo "✅ 服务端TypeScript语法检查通过"
          else
            echo "server_check=failed" >> $GITHUB_OUTPUT
            echo "❌ 服务端TypeScript语法检查失败"
            exit 1
          fi

      - name: 输出语法检查失败信息
        if: failure() && steps.check-files.outputs.has_code_files == 'true'
        run: |
          echo "=== ❌ 自动检查失败 ==="
          echo ""
          echo "您的PR包含TypeScript语法错误，请修复后重新提交。"
          echo ""
          echo "### 检查结果："
          echo "- 客户端检查: ${{ steps.tsc-check-client.outputs.client_check || '未执行' }}"
          echo "- 服务端检查: ${{ steps.tsc-check-server.outputs.server_check || '未执行' }}"
          echo ""
          echo "请运行以下命令在本地检查语法错误："
          echo "# 检查客户端"
          echo "cd client && npx tsc --noEmit"
          echo ""
          echo "# 检查服务端"
          echo "cd server && npx tsc --noEmit"
          echo ""
          echo "修复所有错误后，请重新推送代码。"

      - name: 输出文档变更信息
        if: steps.check-files.outputs.doc_only == 'true'
        run: |
          echo "=== 📝 文档变更检测 ==="
          echo ""
          echo "检测到您的PR仅包含文档文件的变更，无需进行代码语法检查。"
          echo ""
          echo "此PR将等待项目维护者进行最终审核。感谢您对文档的贡献！ 🎉"

      - name: 创建或切换到feature分支（仅主分支PR）
        if: success() && steps.check-files.outputs.has_code_files == 'true' && steps.check-files.outputs.is_main_branch == 'true'
        run: |
          # 配置git用户信息
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # 创建feature分支名称（基于PR编号）
          FEATURE_BRANCH="feature/pr-${{ github.event.number }}"
          echo "FEATURE_BRANCH=$FEATURE_BRANCH" >> $GITHUB_ENV
          echo "准备创建feature分支: $FEATURE_BRANCH"

          # 检查feature分支是否已存在
          if git ls-remote --heads origin $FEATURE_BRANCH | grep -q $FEATURE_BRANCH; then
            echo "Feature分支已存在，切换到该分支"
            git fetch origin $FEATURE_BRANCH
            git checkout $FEATURE_BRANCH
            git merge origin/${{ github.head_ref }} --no-ff -m "Merge latest changes from PR #${{ github.event.number }}"
          else
            echo "创建新的feature分支"
            git checkout -b $FEATURE_BRANCH
          fi

      - name: 推送到feature分支（仅主分支PR）
        if: success() && steps.check-files.outputs.has_code_files == 'true' && steps.check-files.outputs.is_main_branch == 'true'
        run: |
          echo "尝试推送到feature分支..."
          if git push origin $FEATURE_BRANCH; then
            echo "✅ 成功推送到feature分支"
            echo "push_success=true" >> $GITHUB_ENV
          else
            echo "❌ 推送失败，可能是权限问题"
            echo "push_success=false" >> $GITHUB_ENV
            echo "请手动创建feature分支: $FEATURE_BRANCH"
          fi

      - name: 输出检查结果（主分支PR）
        if: always() && steps.check-files.outputs.has_code_files == 'true' && steps.check-files.outputs.is_main_branch == 'true'
        run: |
          echo "=== TypeScript检查结果 ==="
          echo "客户端检查: ${{ steps.tsc-check-client.outputs.client_check || '未执行' }}"
          echo "服务端检查: ${{ steps.tsc-check-server.outputs.server_check || '未执行' }}"
          echo "Feature分支: ${{ env.FEATURE_BRANCH || '未创建' }}"
          echo "推送状态: ${{ env.push_success || '未推送' }}"

          if [[ "${{ steps.tsc-check-client.outputs.client_check }}" == "success" && "${{ steps.tsc-check-server.outputs.server_check }}" == "success" ]]; then
            echo "✅ TypeScript语法检查通过"
            if [[ "${{ env.push_success }}" == "true" ]]; then
              echo "✅ 代码已成功合并到feature分支"
            else
              echo "⚠️  由于权限限制，无法自动创建feature分支"
              echo "请维护者手动创建分支: ${{ env.FEATURE_BRANCH }}"
            fi
          else
            echo "❌ TypeScript语法检查失败"
          fi

      - name: 输出主分支PR检查通过信息
        if: success() && steps.check-files.outputs.has_code_files == 'true' && steps.check-files.outputs.is_main_branch == 'true'
        run: |
          echo "=== ✅ 自动化检查通过（主分支PR） ==="
          echo ""
          echo "您的PR已通过自动化检查！"
          echo ""
          echo "### 检查结果："
          echo "- ✅ 客户端TypeScript语法检查通过"
          echo "- ✅ 服务端TypeScript语法检查通过"
          echo "- ✅ 代码语法验证完成"
          echo ""
          echo "接下来将等待项目维护者进行最终检查，如果没有问题将会合并到主分支并在下个版本发布时加入。"
          echo ""
          echo "感谢您的贡献！ 🎉"

      - name: 输出feature分支PR检查通过信息
        if: success() && steps.check-files.outputs.has_code_files == 'true' && steps.check-files.outputs.is_main_branch == 'false'
        run: |
          echo "=== ✅ 自动化检查通过（feature分支PR） ==="
          echo ""
          echo "您的PR已通过自动化检查，目标分支为: ${{ steps.check-files.outputs.target_branch }}"
          echo ""
          echo "### 检查结果："
          echo "- ✅ 客户端TypeScript语法检查通过"
          echo "- ✅ 服务端TypeScript语法检查通过"
          echo "- ✅ 代码语法验证完成"
          echo ""
          echo "由于您的PR目标是feature分支，将等待项目维护者进行审核和合并。"
          echo ""
          echo "感谢您的贡献！ 🎉"

      - name: 手动运行结果输出
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "=== 手动运行TypeScript检查完成 ==="
          echo "客户端检查结果: ${{ steps.tsc-check-client.outputs.client_check || '未执行' }}"
          echo "服务端检查结果: ${{ steps.tsc-check-server.outputs.server_check || '未执行' }}"

          if [[ "${{ steps.tsc-check-client.outputs.client_check }}" == "success" && "${{ steps.tsc-check-server.outputs.server_check }}" == "success" ]]; then
            echo "✅ 所有TypeScript语法检查通过！"
          else
            echo "❌ 存在TypeScript语法错误，请检查上面的输出"
            exit 1
          fi
